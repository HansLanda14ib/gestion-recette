import React from 'react';
import Layout from './Layout'; // Replace with your actual path

const Home = ({}) => {
    return (
        <div>
            <h1>Welcome to the Recipe App!</h1>
        </div>
    );
};

export default Home;
